#from distutils.core import setup, Extension
from setuptools import setup , find_packages
import numpy as np


setup(
        name = 'ising_block_level_influence',
        version = '1.0',
		author = 'Matthew Garrod',
        packages=['ising_block_level_influence'],
      )
